---
title: Technical Basis and Source Register
status: active
last_reviewed: 2026-07-12
---

# Technical basis and source register

## Purpose

This register records external evidence that materially affects architecture, scope, security, compatibility, or quality gates. It is not a general bibliography. Primary sources are preferred, and version-sensitive findings are reviewed before the affected milestone ships.

## Provider and protocol evidence

| Area | Primary source | Finding | Plan implication | Review trigger |
|---|---|---|---|---|
| Floci release | [Floci 1.5.32 release](https://github.com/floci-io/floci/releases/tag/1.5.32), [installation](https://floci.io/floci/getting-started/installation/), [license](https://github.com/floci-io/floci/blob/main/LICENSE) | The current reviewed release is MIT-licensed and published for Linux amd64 and arm64. The reviewed OCI index digest is `sha256:4f69631e560120d79ad82d2af9f7dda8c6ef7ecbbae0c43ddcffa109c6588a15`. | Allowlist the exact tag and digest in code/schema; review license, digest, compatibility, and tests on every upgrade. | On Floci upgrade |
| Floci IAM | [Floci IAM documentation](https://floci.io/floci/services/iam/) | IAM enforcement is opt-in. It evaluates identity, session, and boundary policies, but unknown keys and unmapped operations are permissive; resource policies and `NotPrincipal` are unsupported. | Enable enforcement, use only declared synthetic identities, and bound every authorization claim in the compatibility matrix. | On Floci upgrade or M1 claim change |
| Floci STS | [Floci STS documentation](https://floci.io/floci/services/sts/) | IAM enforcement is opt-in; STS trust evaluation omits `Condition` blocks and caller-side `sts:AssumeRole` authorization. | CloudAILab owns authoritative scenario policy semantics and documents gaps. | Before M1 and on Floci upgrade |
| Floci accounts | [Floci multi-account isolation](https://floci.io/floci/configuration/multi-account/) | Twelve-digit access-key IDs select account namespaces; temporary credentials route to the assumed account; signature validation is off by default. | Use deliberate test credentials and explicitly configure signature behavior. | Before M1 and on Floci upgrade |
| AWS policy semantics | [AWS IAM policy evaluation logic](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_evaluation-logic.html) | AWS combines multiple policy types, uses implicit deny by default, and gives applicable explicit denies precedence. | Model only declared IAM semantics, preserve deny precedence, and document every omitted policy type. | Before M1 policy claims |
| AWS cross-account access | [AWS cross-account evaluation](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_evaluation-logic-cross-account.html) | Cross-account access commonly requires permission in both the trusted principal's account and the trusting resource's account. | Test both sides of supported cross-account paths instead of relying on Floci's trust-policy-only behavior. | Before M1 exit |
| Microsoft simulation | [Dev Proxy overview](https://learn.microsoft.com/en-us/microsoft-cloud/dev/dev-proxy/overview) | Dev Proxy focuses on API simulation, resilience, failures, throttling, and Graph guidance. | Use as optional compatibility/chaos tooling, not Entra source of truth. | Before any Dev Proxy integration |
| Microsoft CRUD | [Dev Proxy CRUD simulation](https://learn.microsoft.com/en-us/microsoft-cloud/dev/dev-proxy/how-to/simulate-crud-api) | CrudApiPlugin supplies generic in-memory CRUD state. | Native facade remains preferable for canonical identity and authorization semantics. | Before M2 |
| Microsoft TLS | [Dev Proxy troubleshooting](https://learn.microsoft.com/en-us/microsoft-cloud/dev/dev-proxy/how-to/troubleshooting) | HTTPS interception requires certificate trust, with runtime-specific handling. | Default mode avoids host-wide interception and global trust changes. | Before advanced proxy mode |
| Graph clients | [Customize Microsoft Graph SDK client](https://learn.microsoft.com/en-us/graph/sdks/customize-client) | SDK middleware and client behavior can be customized for testing. | Publish supported endpoint/client examples rather than requiring transparent interception. | Before M2 SDK examples |
| Graph directory collections | [List users](https://learn.microsoft.com/en-us/graph/api/user-list?view=graph-rest-1.0), [list applications](https://learn.microsoft.com/en-us/graph/api/application-list?view=graph-rest-1.0), [list service principals](https://learn.microsoft.com/en-us/graph/api/serviceprincipal-list?view=graph-rest-1.0) | Graph v1.0 returns directory collections in `value` envelopes, documents selected fields, bearer authorization, OData options, page sizes, and next links. | Implement and test only scenario-required fields, pagination, and errors; reject unsupported queries explicitly. | On M2 contract change or Graph documentation change |
| Graph delegated grants | [OAuth2 permission grant resource](https://learn.microsoft.com/en-us/graph/api/resources/oauth2permissiongrant?view=graph-rest-1.0) | A grant identifies the client service principal, consent type, user principal, resource service principal, and space-separated scopes; the resource supports list, get, create, update, and delete. | Preserve those identifiers, normalize each grant independently, and support tested list/get/delete operations first. | On M2 consent contract change |
| Graph app roles | [Grant an app role assignment to a group](https://learn.microsoft.com/en-us/graph/api/group-post-approleassignments?view=graph-rest-1.0) | Graph models group app-role assignments using principal, resource service principal, and app-role identifiers. | Preserve those identifiers and relationships in the Microsoft facade and canonical graph. | Before M2 application contracts |
| Graph app-role inventory and deletion | [App-role assignment resource](https://learn.microsoft.com/en-us/graph/api/resources/approleassignment?view=graph-rest-1.0), [list resource assignments](https://learn.microsoft.com/en-us/graph/api/serviceprincipal-list-approleassignedto?view=graph-rest-1.0), [delete resource assignment](https://learn.microsoft.com/en-us/graph/api/serviceprincipal-delete-approleassignedto?view=graph-rest-1.0) | A group assignment relates `principalId`, resource service-principal `resourceId`, and declared `appRoleId`; the resource-side delete returns 204. | Normalize each live assignment independently and use resource-side deletion for the flagship remediation. | On M2 Microsoft contract change |
| Google Directory users and groups | [users.list](https://developers.google.com/workspace/admin/directory/reference/rest/v1/users/list), [groups.list](https://developers.google.com/workspace/admin/directory/reference/rest/v1/groups/list) | Directory v1 uses `users` or `groups` envelopes, `nextPageToken`, customer/domain scoping, and maximum page sizes of 500 users and 200 groups. | Implement only scenario-required fields, deterministic local pagination, and explicit unsupported-query errors. | On M2 Directory contract change |
| Google Directory members | [members.list](https://developers.google.com/workspace/admin/directory/reference/rest/v1/members/list) | Direct group membership is addressed by group ID/email and returns roles and member types in a `members` envelope. | Model direct `USER` membership first and normalize it separately from file authorization. | On M2 membership contract change |
| Google Drive files | [files.list](https://developers.google.com/workspace/drive/api/reference/rest/v3/files/list), [files.get](https://developers.google.com/workspace/drive/api/reference/rest/v3/files/get) | Drive v3 returns `files` and `nextPageToken`; `files.get` with `alt=media` returns stored file content. | Implement selected metadata, content read, and deterministic pagination; document omitted search, hierarchy, and export behavior. | On M2 Drive file contract change |
| Google Drive permissions | [permissions.list](https://developers.google.com/workspace/drive/api/reference/rest/v3/permissions/list), [permissions.delete](https://developers.google.com/workspace/drive/api/reference/rest/v3/permissions/delete) | Permissions identify role and principal type; list uses a `permissions` envelope and delete removes a file permission. | Support direct scenario user/group grants, normalize each grant independently, and test deletion while preserving group-derived access. | On M2 Drive authorization change |
| OIDC and OAuth metadata | [OpenID Connect Discovery 1.0](https://openid.net/specs/openid-connect-discovery-1_0.html), [RFC 8414](https://www.rfc-editor.org/rfc/rfc8414) | Metadata publishes an exact issuer, endpoints, supported capabilities, and `jwks_uri`; the standards require HTTPS issuer and metadata URLs. | Implement standard-shaped metadata but label random loopback HTTP as an intentional local-development non-conformity. | On local issuer contract or TLS-mode change |
| OAuth Authorization Code | [RFC 6749](https://www.rfc-editor.org/rfc/rfc6749) | Token requests use POST, confidential clients authenticate, authorization codes bind to client and redirect URI, and OAuth defines stable error codes; token transport requires TLS. | Use Basic-authenticated, one-time, short-lived synthetic codes with exact loopback redirect matching; never claim production OAuth transport conformance. | On local grant-flow change |
| JWT claims and validation | [RFC 7519](https://www.rfc-editor.org/rfc/rfc7519), [RFC 8725](https://www.rfc-editor.org/rfc/rfc8725), [RFC 9068](https://www.rfc-editor.org/rfc/rfc9068) | JWT profiles require application-specific algorithm, type, signature, issuer, audience, and time validation; RFC 9068 uses `at+jwt` and required access-token claims. | Pin RS256 and explicit types; validate exact issuer/audience and time claims; keep ID and access-token rules mutually exclusive. | On token profile or claim change |
| JWKS and rollover | [RFC 7517](https://www.rfc-editor.org/rfc/rfc7517), [RFC 7638](https://www.rfc-editor.org/rfc/rfc7638) | JWK Sets publish public key material; distinct `kid` values select rollover keys; thumbprints canonically identify keys. | Derive `kid` from the RSA public JWK and retain retired public keys until all possibly signed tokens expire. | On algorithm, key, or rotation change |
| AWS web identity | [AWS OIDC temporary credentials](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_temp_request.html), [federated principals](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_elements_principal.html) | AWS normally validates an OIDC token and role trust before returning temporary credentials. A local spike against pinned Floci 1.5.32 returned credentials for `not-a-signed-jwt` despite no matching federated trust. | CloudAILab validates its active issuer, token, live Microsoft assignment, and typed web trust before calling Floci; direct Floci calls are not authorization evidence. | On Floci upgrade or federation contract change |

## AI security and governance evidence

| Area | Primary source | Finding | Plan implication | Review trigger |
|---|---|---|---|---|
| AI risk lifecycle | [NIST AI RMF](https://www.nist.gov/itl/ai-risk-management-framework) | AI RMF structures risk work around Govern, Map, Measure, and Manage; version 1.0 is under revision. | CAL uses the framework for control alignment, not certification, and tracks revisions. | Before M3 and framework revision |
| AI evaluation | [NIST AI RMF Core](https://airc.nist.gov/airmf-resources/airmf/5-sec-core/) | TEVV should be objective, repeatable/scalable, documented, and relevant to deployment conditions; uncertainty and limitations matter. | Agent reports include metrics, run context, repeated trials, errors, and limitations; automatic fixture campaigns restore and verify deployment state before every trial. | Before M3 metric freeze |
| AI metric transparency | [NIST AI RMF Playbook: Measure 2.1](https://airc.nist.gov/airmf-resources/playbook/measure/), [NIST Metrics and Measures](https://www.nist.gov/itl/ai/ai-standards-and-guidelines-group/metrics-and-measures) | Test sets, tools, metrics, and methodology details support repeatability; objective measures are typically more automatable and repeatable, while measurement context and unmeasured characteristics must remain visible. | Replay emits primitive evidence-derived counts with denominators/rates, configuration digests, and stable unavailable-metric reasons instead of a subjective composite score. | On evaluation profile or measured-construct change |
| Human-AI oversight | [NIST AI RMF Core](https://airc.nist.gov/airmf-resources/airmf/5-sec-core/), [Appendix C](https://airc.nist.gov/airmf-resources/airmf/appendices/app-c-ai-risk-management-and-human-ai-interaction/), [AI RMF Playbook: Measure](https://airc.nist.gov/airmf-resources/playbook/measure/) | Human-AI oversight roles and responsibilities should be differentiated and documented, and downstream overrides or adjudication should be measurable. | Approval evidence records the accountable resolver, exact decision context, outcome, and linkage without allowing human approval to bypass deterministic policy. | On approval or oversight workflow change |
| Agent threats | [OWASP Top 10 for Agentic Applications 2026](https://genai.owasp.org/resource/owasp-top-10-for-agentic-applications-for-2026/) | Current agentic risks include goal hijacking, tool misuse, identity/privilege abuse, supply chain, and unexpected code execution. | Flagship scenario must cover the first three; threat model tracks the broader set. | Before M3 and annual OWASP update |
| Agent evaluation probes | [NIST agentic evaluation probes](https://www.nist.gov/programs-projects/building-evaluation-probes-agentic-ai) | NIST is developing workflow-integrated adversarial verifiers and structured audit trails that connect decisions to evidence. | Keep action-level decisions and evidence correlation inside the deterministic gateway rather than relying on post-hoc model narration. | Before M3 trace stability |
| Agent hijacking evaluation | [NIST: Strengthening AI Agent Hijacking Evaluations](https://www.nist.gov/news-events/news/2025/01/technical-blog-strengthening-ai-agent-hijacking-evaluations) | NIST frames a hijacking scenario around a legitimate task, attacker-controlled data, and whether the agent completes the malicious injection task; repeated attempts matter for probabilistic agents. | Require proven exposure, label the prohibited task, separate attempt from success, and aggregate automatic unsafe campaigns with explicit denominators. | On adversarial profile or campaign-runner change |
| Indirect prompt injection | [OWASP LLM01:2025 Prompt Injection](https://genai.owasp.org/llmrisk/llm01-prompt-injection/) | Indirect injection arrives through external sources such as files and can lead to unauthorized function use or disclosure; least privilege and trust boundaries limit impact. | Deliver inert synthetic instructions through the Google-shaped file surface and measure agent behavior separately from gateway containment. | On fixture, tool, or policy-boundary change |
| JSON Lines | [JSON Lines format](https://jsonlines.org/) | Each UTF-8 line is a complete JSON value and a final line terminator is recommended. | Use one bounded JSON object per subprocess frame and reserve stdout for protocol traffic. | On agent transport change |
| JSON Schema | [JSON Schema Draft 2020-12](https://json-schema.org/draft/2020-12) | Core and validation vocabularies provide a portable machine-readable structural contract. | Publish v1alpha1 schemas for tools, runs, messages, and decision events; keep typed Go validation authoritative. | On agent schema version change |
| MCP tools | [MCP tool contract](https://modelcontextprotocol.io/specification/2025-06-18/server/tools) | MCP tools declare JSON input schemas for model-controlled invocation. | Use strict JSON Schema inputs in CloudAILab manifests while deferring an MCP adapter and any compatibility claim. | Before MCP adapter work |
| JSON redaction paths | [RFC 6901](https://www.rfc-editor.org/rfc/rfc6901.html) | JSON Pointer provides an unambiguous string syntax for selecting values, including escaped `/` and `~`. | Declare sensitive fields and decision redactions as validated pointers; fail when a requested path does not exist. | On redaction contract change |
| JSON canonicalization | [RFC 8785](https://www.rfc-editor.org/rfc/rfc8785.html) | Reproducible hashing requires an invariant representation of JSON. | Normalize manifest JSON before SHA-256 hashing, but do not claim full JCS conformance in v1alpha1. | Before stable protocol release |
| AI control catalog | [CSA AI Controls Matrix v1.1](https://cloudsecurityalliance.org/artifacts/ai-controls-matrix-v1-1) | AICM provides vendor-neutral cloud AI control objectives and mappings to other standards. | Scenario metadata may map measured controls to AICM identifiers without claiming compliance. | Before control mapping release |

## Engineering and supply-chain evidence

| Area | Primary source | Finding | Plan implication | Review trigger |
|---|---|---|---|---|
| Go security | [Go security best practices](https://go.dev/doc/security/best-practices) | Go recommends vulnerability analysis, fuzzing, and race detection as complementary practices. | Add govulncheck, fuzz targets, and race tests at defined CI cadences. | On Go toolchain upgrade |
| Go modules | [Go Modules reference](https://go.dev/ref/mod) | `go mod tidy -diff` reports required module-file changes without modifying files, and `go mod verify` checks downloaded module content. | Commit module metadata and require tidy/integrity checks in CI. | On Go toolchain or dependency policy change |
| Go fuzzing | [Go fuzzing](https://go.dev/doc/security/fuzz/) | Fuzz targets should be fast, deterministic, and free of persistent cross-call state. | Apply fuzzing to parsers, policies, identifiers, tokens, and event decoding. | When adding untrusted input surface |
| Go subprocess lifecycle | [Go `os/exec` package](https://pkg.go.dev/os/exec), [`CommandContext` and `WaitDelay` source documentation](https://go.dev/src/os/exec/exec.go) | `CommandContext` cancels a direct child through `Process.Kill` by default; `WaitDelay` bounds waits caused by a non-exiting child or unclosed communication pipes. It does not define descendant-process containment. | Own, cancel, and wait for each direct agent child; set a bounded `WaitDelay`; document descendant and isolation limits separately. | On Go toolchain or agent execution-backend change |
| Docker container boundary | [Running containers](https://docs.docker.com/engine/containers/run/), [none network driver](https://docs.docker.com/engine/network/drivers/none/), [Docker Engine security](https://docs.docker.com/engine/security/) | Docker creates separate filesystem, network, and process namespaces; the none driver creates only private loopback. The Docker daemon and host kernel remain trusted attack surfaces. | The opt-in agent adapter uses no host mounts or published ports, network none, non-root execution, dropped capabilities, and accurately bounded containment claims. | On Docker minimum version or isolation flags change |
| Docker least privilege | [Seccomp profiles](https://docs.docker.com/engine/security/seccomp/), [container run options](https://docs.docker.com/reference/cli/docker/container/run) | Docker's built-in seccomp profile is an allowlist-style default; `no-new-privileges`, `cap-drop`, and explicit built-in seccomp selection prevent daemon-default weakening from silently widening a run. | Select built-in seccomp explicitly, drop all capabilities, and enable no-new-privileges for every isolated agent. | On Docker or runtime security-profile change |
| Docker resource and storage limits | [Resource constraints](https://docs.docker.com/engine/containers/resource_constraints/), [tmpfs mounts](https://docs.docker.com/engine/storage/tmpfs/) | Containers are unconstrained by default; Docker exposes memory/CPU limits, while tmpfs supports size, noexec, nosuid, nodev, UID, and GID controls. | Apply fixed memory/CPU/PID/file limits, a read-only root, and one bounded temporary filesystem. | On workload requirements or limit change |
| Docker image identity | [Pull by digest](https://docs.docker.com/reference/cli/docker/image/pull/) | Image IDs and repository digests are content-addressed; mutable tags do not identify stable bytes. Digest pinning also prevents automatic security updates until the identifier changes. | Accept only local SHA-256 image IDs or repository digests, never pull implicitly, and persist the exact reference in run metadata. | On agent artifact or supply-chain design change |
| Docker container logging | [Configure logging drivers](https://docs.docker.com/engine/logging/configure/), [container run options](https://docs.docker.com/reference/cli/docker/container/run) | The daemon commonly defaults to `json-file`; `--log-driver none` disables container log retrieval while attached standard streams remain available to the controller. | Disable Docker log persistence for agent protocol and diagnostic streams; retain only CloudAILab's bounded in-memory diagnostics and evidence-safe records. | On agent transport or diagnostic retention change |
| Docker contexts | [Docker contexts](https://docs.docker.com/engine/manage-resources/contexts/), [Docker CLI host option](https://docs.docker.com/reference/cli/docker/) | Contexts and environment variables can redirect the CLI to Unix, TCP, SSH, or named-pipe daemon endpoints; `--host` pins a command to one explicit endpoint. | Inspect the active endpoint, accept only an absolute local Unix socket with a Linux server, pin it for run and cleanup, and reject remote contexts before trial persistence. | On engine or supported-host expansion |
| Docker image volumes and IPC | [Dockerfile `VOLUME`](https://docs.docker.com/reference/dockerfile#volume), [container read-only and IPC options](https://docs.docker.com/reference/cli/docker/container/run) | An image-declared volume creates writable storage at run time even with a read-only root; IPC `none` uses a private namespace without mounting `/dev/shm`. | Reject images with declared volumes and select IPC none so `/tmp` is the only intentional application-writable memory filesystem. | On image contract or writable-storage change |
| Docker rootless cgroups | [Rootless mode resource-limit guidance](https://docs.docker.com/engine/security/rootless/tips/#limiting-resources) | Rootless engines can ignore CPU, memory, and PID flags when cgroup v2/systemd controller delegation is unavailable or incomplete. | Reject rootless engines and require an active cgroup driver for the initial fixed-resource isolation contract. | On rootless or resource-verification support |
| Docker fixed loopback publication | [Docker container run](https://docs.docker.com/reference/cli/docker/container/run/#published-ports) | Docker supports publishing a container port to a specified host address and port; omitting the host address can expose it on all interfaces. | Replace only the ownership-checked Floci container and republish port 4566 at the previously recorded `127.0.0.1` host port so endpoint configuration remains stable. | On provider restoration or Docker port-publication behavior change |
| Floci memory state | [Floci environment variables](https://floci.io/floci/configuration/environment-variables/) | Floci supports an explicit `memory` storage mode and documents it as the default global backend. | Set `FLOCI_STORAGE_MODE=memory` explicitly so the replacement provider begins empty before deterministic rehydration. | On Floci upgrade or storage-mode change |
| Go JSON Schema validation | [`jsonschema/v6` v6.0.2](https://pkg.go.dev/github.com/santhosh-tekuri/jsonschema/v6@v6.0.2), [JSON Schema Draft 2020-12](https://json-schema.org/draft/2020-12) | The pinned Apache-2.0 library implements Draft 2020-12 and supports an explicit default draft and resource loader. | Compile tool schemas with Draft 2020, no external loader, and fragment-local references only; review license, vulnerabilities, and behavior on upgrade. | On validator upgrade or input-schema contract change |
| SQLite decision evidence | [SQLite transactions](https://www.sqlite.org/lang_transaction.html), [table constraints](https://www.sqlite.org/lang_createtable.html), [transactional guarantees](https://www.sqlite.org/transactional.html) | SQLite serializes writes and provides transactional commit; primary, unique, check, and not-null constraints can enforce local record invariants. | Allocate event sequence, insert canonical evidence, and advance the chain head in one transaction; also verify the chain in application code. | On state schema or SQLite driver change |
| YAML parser | [`go.yaml.in/yaml/v3`](https://pkg.go.dev/go.yaml.in/yaml/v3) | The package is maintained by the YAML organization and supports strict known-field decoding. | Pin the parser and reject unknown scenario fields. | On parser upgrade |
| SQLite driver | [`modernc.org/sqlite`](https://pkg.go.dev/modernc.org/sqlite) | The driver provides a CGO-free `database/sql` implementation across the target desktop platforms. | Use it for local transactional state and verify every target in CI. | On driver or Go upgrade |
| AWS SDK for Go | [AWS SDK for Go v2 documentation](https://docs.aws.amazon.com/sdk-for-go/v2/developer-guide/welcome.html) | The official v2 SDK supports service-specific clients, static credential providers, and custom service endpoints. | Use pinned IAM, STS, and S3 modules for typed hydration and contract tests; never discover host AWS credentials. | On SDK upgrade |
| Workflow pinning | [GitHub Actions settings](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/enabling-features-for-your-repository/managing-github-actions-settings-for-a-repository) | Repositories can require actions to be pinned to full-length commit SHAs. | Pin actions and minimize workflow permissions from the first workflow. | On workflow addition/update |
| Build provenance | [GitHub artifact attestations](https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations) | GitHub supports provenance attestations for binaries, images, and SBOMs. | M4 release artifacts include checksums, SBOM, and attestations. | Before M4 release |
| Provenance model | [SLSA provenance](https://slsa.dev/spec/v1.2/provenance) | Provenance links artifacts to where, when, and how they were produced. | Release documentation explains artifact verification and build origin. | On SLSA spec change |
| Policy testing | [OPA policy testing](https://www.openpolicyagent.org/docs/policy-testing) | OPA provides a declarative policy language and test framework. | OPA is evaluated in M0 but not assumed until an ADR accepts it. | During M0 policy spike |
| Telemetry | [OpenTelemetry semantic conventions](https://opentelemetry.io/docs/concepts/semantic-conventions/) | Common conventions provide consistent names across traces, metrics, logs, and resources. | Reuse stable names where applicable while keeping CAL's event schema portable. | Before event schema stability |
| OpenTelemetry events | [Event semantic conventions](https://opentelemetry.io/docs/specs/semconv/general/events/) | Events represent distinct occurrences with their own name, timestamp, severity, and occurrence-specific attributes. | Keep decision events exporter-independent and map them to telemetry only after the portable schema stabilizes. | Before telemetry export |

## Source quality rules

- Prefer vendor documentation, standards bodies, and project documentation over blogs or search summaries.
- Record the exact limitation, not a generalized product judgment.
- Do not copy marketing performance claims into requirements without independent measurement.
- Cite version-sensitive facts near the corresponding plan or compatibility claim.
- If a source conflicts with observed behavior, preserve the observed test evidence and document the discrepancy.
- Review this register at each milestone exit and before dependency upgrades that affect recorded behavior.
